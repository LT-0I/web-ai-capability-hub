# share/export-conversation-markdown

Status: NOT-REACHABLE

The conversation Share dialog on `chatgpt.com` exposes only public-link +
3 social-platform buttons (`Copy link / X / LinkedIn / Reddit`). It does
NOT expose `Export as Markdown`, `Export to Docs`, `Export to PDF`, or any
private-export option.

ChatGPT's only conversation-data-export surface is at Settings → Data
controls → `Export data Export` (account-wide email-based export — also
NOT clicked because it triggers a delivery flow). See
`settings/data-controls.json`.

NOT-REACHABLE recorded.

Catalog feedback: `chat-share-link-create` should note that the only
text-export options inside the per-conversation Share dialog are public
social-share targets; private DOCX/Markdown export is not available.
