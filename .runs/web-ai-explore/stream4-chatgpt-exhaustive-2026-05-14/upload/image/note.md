# upload/image

Status: PASS

## Observation

Tab `s4-up-img`. File `smoke-image.png` uploaded via `input#upload-files`.
Chip in DOM: `Open image: smoke-image.png` (image opens in a viewer).

Q1: `Describe this image in one sentence.`
A1: `A solid red square fills the image.` (correct: smoke-image.png is a
180-byte red PNG).

Q2: `What color is it? One word.`
A2: `Red` (correct).

Evidence: `read-q1.json`, `read-q2.json`.
