# A3 — new-conversation

**Status:** PASS

Allocated a fresh tab to `https://claude.ai/new`. Waited for the composer
`div[contenteditable="true"]` selector to become available, then read the page
in lite mode. URL captured: `https://claude.ai/new`. Composer is empty
(no pre-filled text). The standard claude.ai/new landing — `Write your prompt
to Claude` placeholder — was confirmed in A0.

Evidence: `read-empty.json`.
