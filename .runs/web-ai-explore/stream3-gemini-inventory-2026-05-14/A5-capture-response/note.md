# A5 — capture-response

**Status:** PASS

First 500 chars of model reply (entire reply was shorter) captured to
`response.txt`. Text is clearly the model's reply (not UI strings, not empty),
contains today's date `Thursday, May 14, 2026`.

Evidence: `response.txt` (sourced from
`$RUN_DIR/A4-send-test-message/post-send-read-full.stdout.json`).
