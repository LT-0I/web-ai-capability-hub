# A3 — new-conversation

**Status:** PASS

The tab allocated for A2 (`A2-model`) opens at `https://gemini.google.com/app`
which is the empty new-chat home page. The composer is empty (no prior turns
visible in DOM read), and `Enter a prompt for Gemini` placeholder is present.
Reused as the conversation surface for A4 and A5.
