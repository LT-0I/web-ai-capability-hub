# settings/my-stuff (/library)

Status: PASS

URL: `/library?hl=en`. Page title "Google Gemini". Catalog row
`my-stuff-folder` resolved with URL.

Items visible in this run:
1. **"Gemini 3 Fast vs. Pro Comparison"** — auto-saved Deep Research
   report (created by the Export-to-Docs action earlier this session
   inside `generate/deep-research`). Has full title plus first 200 chars
   of the report.
2. **"Stream 4 Stability Brief"** — Canvas document created in
   `generate/canvas-text`.

Confirms that **Canvas + Deep Research outputs auto-populate the /library
collection**, even when the user did not explicitly click "Save". This is
a Stream #3 catalog addition `my-stuff-folder` validation.

Stream #3 evidence noted a `Media` tab here showing thumbnails of
generated images. Re-verification across this run did not require
re-exercising — already established.
