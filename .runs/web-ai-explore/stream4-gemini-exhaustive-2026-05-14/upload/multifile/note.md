# upload/multifile

Status: PASS

Three files (`smoke-text.txt`, `smoke-data.csv`, `smoke-image.png`) attached
via three sequential `Open upload file menu` → `Upload files` clicks. The
composer accepted all three chips simultaneously.

Single composite question spanning all 3 files returned correct answers for
each sub-question. Confirms multi-file context handling.
