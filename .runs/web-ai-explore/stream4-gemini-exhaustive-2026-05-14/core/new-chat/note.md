# core/new-chat

Status: PASS

Fresh tab `/app?hl=en` opened by `browser:tab:alloc`. Composer was empty
in locale-enforce snapshot (no value in `div[role="textbox"]`). After
send-receive, URL transitioned from `/app?hl=en` to
`/app/f825b103a6378ea8?hl=en` — confirming new chat created on-send.
