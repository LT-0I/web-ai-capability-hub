# core/send-receive

Status: PASS

Composer typed via
`div[role="textbox"][aria-label="Enter a prompt for Gemini"]`, send via
`button[aria-label="Send message"]` (sensitivity-guard `--confirmed true`).
URL changed to `/app/f825b103a6378ea8` (new chat row "Stream 4 Lane 3 Probe
Message Received" in side panel). Response captured to `response.txt`.
