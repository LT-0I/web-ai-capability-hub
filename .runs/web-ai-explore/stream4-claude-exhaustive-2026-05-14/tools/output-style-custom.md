status: INCONCLUSIVE
observation: Custom Style entry exists: "Create & edit styles" menuitem (#base-ui-_r_9l_) is reachable from composer "+" → "Use style" → bottom of menu. NOT clicked this run because the flow could write a new persistent custom-style to the account. The form is gated behind that menuitem.
followup_needed: A future run could open the form and capture the schema without saving (click Cancel/X to back out).
