status: NOT-REACHABLE
observation: Claude's Share modal does NOT expose a "Copy link" affordance that returns a URL without first creating a public link. The only path is the "Create share link" button which publishes the chat. Per doctrine §3 forbidden-actions list, this run did not click "Create share link".
