# Locale enforcement record — ChatGPT lane

Pre-state: UI in `简体中文` (Simplified Chinese, Settings → General → Language `简体中文`).
Action: Settings → General → Language combobox → `English (US)`.
Post-state: UI chrome flipped to English. User-created chat/project names remain in original Chinese (user content, not UI chrome).

Pre-state composer placeholder (verbatim + translation):
- `与 ChatGPT 聊天` → "Chat with ChatGPT"
- `添加文件等` → "Add files etc."
- `开始听写` → "Start dictation"
- `启动语音功能` → "Start voice"

Post-state composer placeholder: read in checkpoint A3.

Result: PASS — English locale enforced.
