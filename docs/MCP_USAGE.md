# MCP usage

This file is kept for compatibility. The detailed MCP reference is in `docs/MCP_INTEGRATION.md`.

```bash
node dist/src/cli.js mcp:tools --json
node dist/src/cli.js mcp:resources --json
npm run mcp
```
