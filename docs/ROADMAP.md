# Roadmap

- Native foreground scheduler that reads `scheduled_jobs` and runs refresh jobs.
- Richer SQLite-backed FTS ranking when `better-sqlite3` is installed.
- Optional dashboard for reviewing capability diffs and approval gates.
- More web AI adapters as fixtures and user-approved workflows become available.
- More research database adapter profiles generated from imported registry entries.
- Optional visual evidence comparison for changed UI regions.
