# Adapters and recipes

Adapters describe service targets, stable semantic anchors, capability hints, and safety notes. Recipes are legacy JSON-compatible YAML action sequences. New workflow DSL files live in `examples/workflows/` and reference capabilities.

Use adapter notes under `configs/adapters/notes/<site>.md` for UI changes. Capture site maps before updating selectors.
